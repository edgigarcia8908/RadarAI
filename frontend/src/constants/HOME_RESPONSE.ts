export const HOME_RESPONSE_COPY = {
  emptyResponse: 'RadarAI no devolvió contenido para esta consulta. Intenta formular la pregunta de otra manera.',
} as const;

export const HOME_RESPONSE_LIMITS = {
  maxProviders: 5,
  maxProgressPercentage: 100,
} as const;
